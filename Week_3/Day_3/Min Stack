class MinStack {
public:
    stack<long long> st;
    long long minimum=INT_MAX;
    MinStack() {
        
    }
    
    void push(long long val) {
        if(st.empty()) {
            st.push(val);
            minimum=val;
        } else  {
            if(val<minimum) {
                st.push(1LL*2*val-minimum);
                minimum=val;
            } else {t
                st.push(val);
            }
        }
    }
    
    void pop() {
        if(st.empty()) return;
        long long val=st.top();
        if(val<minimum) minimum=2*minimum-st.top();
        st.pop();
    }
    
    int top() {
        if(st.empty()) return -1;
        long long val=st.top();
        if(val<minimum) return minimum;
        else return val;
    }
    
    int getMin() {
        return minimum;
    }
};

/**
 * Your MinStack object will be instantiated and called as such:
 * MinStack* obj = new MinStack();
 * obj->push(val);
 * obj->pop();
 * int param_3 = obj->top();
 * int param_4 = obj->getMin();
 */
